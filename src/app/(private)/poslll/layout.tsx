import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: "CLJ's lll",
  description: "Página dos clj's pos lll",
};

export default function PoslllLayout({ children }: { children: React.ReactNode }) {
  return children;
}
